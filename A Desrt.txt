local DiscordLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/UI-Libs/main/discord%20lib.txt"))()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local holding = LocalPlayer.PlayerGui.main.handtarget1
local twohands = LocalPlayer.PlayerGui.main.twohands

local health = LocalPlayer.PlayerGui.main.health
local hunger = LocalPlayer.PlayerGui.main.hunger
local stamina = LocalPlayer.PlayerGui.main.stamina

local weldinvoke = game:GetService("ReplicatedStorage"):WaitForChild("weld")
local changehand = game:GetService("ReplicatedStorage"):WaitForChild("changehandtarget")
local function weld(weldto)
    local weldtarget

    for _,obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") or obj:IsA("Model") then
            local welds = 0
            local foundgyro = false
            local foundpos = false
            local foundweld

            for _,v in pairs(obj:GetChildren()) do
                if v:IsA("WeldConstraint") and v.Name:lower():find("objectweld") then
                    welds += 1
                    foundweld = v
                elseif v:IsA("BodyGyro") then
                    foundgyro = true
                elseif v:IsA("BodyPosition") then
                    foundpos = true
                end
            end

            if welds >= 2 and foundgyro and foundpos and foundweld then
                if foundweld.Part1 then
                    weldtarget = foundweld.Part1
                    print("Found weldtarget:", weldtarget)
                    break
                end
            end
        end
    end

    if not weldtarget then
        warn("No weld target found")
        return
    end

    if twohands.Value then
        changehand:FireServer("hand2", false)
        changehand:FireServer("hand1", false)
    else
        changehand:FireServer("hand2", false)
    end

    weldinvoke:InvokeServer(unpack({
        weldtarget,
        weldtarget.Parent,
        {
            {
                obj = weldto,
                n = 1
            }
        }
    }))
end

local function GetClosestCharacter()
	local closest, dist = nil, math.huge
	local root = LocalPlayer.Character.HumanoidRootPart

	for _,v in pairs(Players:GetPlayers()) do
		if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
			local mag = (root.Position - v.Character.HumanoidRootPart.Position).Magnitude

			if mag < dist then
				dist = mag
				closest = v.Character
			end
		end
	end

	return closest
end

local function GetClosestCar() -- fucking quit workspace.nearestcar is a option fuck
	local closest, dist = nil, math.huge
	local root = LocalPlayer.Character.HumanoidRootPart

	for _,v in pairs(workspace:GetDescendants()) do
		if v:IsA("Model") and v.Name:lower():find("car") and v.PrimaryPart then
			local mag = (root.Position - v.PrimaryPart.Position).Magnitude

			if mag < dist then
				dist = mag
				closest = v
			end
		end
	end

	return closest
end

local function GetClosestWheel()
	local closest, dist = nil, math.huge
	local root = LocalPlayer.Character.HumanoidRootPart

	for _,folder in pairs(workspace:GetDescendants()) do
		if folder:IsA("Folder") and folder.Name == "Wheels" then
			for _,v in pairs(folder:GetChildren()) do
				if v:IsA("BasePart") then
					local mag = (root.Position - v.Position).Magnitude

					if mag < dist then
						dist = mag
						closest = v
					end
				end
			end
		end
	end

	return closest
end

local toggledstuff = {}

function toggle(value, toggle)
	if toggle then
		if toggledstuff[value] then
			return
		end

		toggledstuff[value] = value.Changed:Connect(function()
			value.Value = 100
		end)

		value.Value = 100
	else
		if toggledstuff[value] then
			toggledstuff[value]:Disconnect()
			toggledstuff[value] = nil
		end
	end
end

local win = DiscordLib:Window("kltekk is fire - A Desrt")

local WeldingTab = win:Server("Welding", "http://www.roblox.com/asset/?id=76198848375006")
local InfiniteTab = win:Server("Player Related", "http://www.roblox.com/asset/?id=9421679578")
local WeldingBinds = WeldingTab:Channel("Welding Binds")
local PlayerChannel = InfiniteTab:Channel("Player Attributes")
local InfiniteCreds = InfiniteTab:Channel("Credits")
local WeldingCreds = WeldingTab:Channel("Credits")
local WeldCreds = WeldingCreds:Label("Made by @dudeimnotzelda123 on discord")
local InfCreds = InfiniteCreds:Label("Made by @dudeimnotzelda123 on discord")
local WeldCredsButton = WeldingCreds:Button("Discord", function()
    DiscordLib:Notification("Discord Invite saved to clipboard", "yo so twin click that lil button", "that lil button")
    setclipboard("https://discord.gg/qx62tWVmB2")
end)
local InfCredsButton = InfiniteCreds:Button("Discord", function()
    DiscordLib:Notification("Discord Invite saved to clipboard", "yo so twin click that lil button", "that lil button")
    setclipboard("https://discord.gg/qx62tWVmB2")
end)
PlayerChannel:Toggle("Infinite Health", false, function(bool)
    toggle(health, bool)
end)

PlayerChannel:Toggle("Infinite Hunger", false, function(bool)
    toggle(hunger, bool)
end)

PlayerChannel:Toggle("Infinite Stamina", false, function(bool)
    toggle(stamina, bool)
end)

WeldingBinds:Bind("Weld To Nearest Player", Enum.KeyCode.Unknown, function()
    local char = GetClosestCharacter()

    if char and char:FindFirstChild("HumanoidRootPart") then
        weld(char.HumanoidRootPart, false)
    end
end)

WeldingBinds:Bind("Weld Anywhere", Enum.KeyCode.Unknown, function()
    local target = workspace.undelete.mainhouse.model:FindFirstChild("Part")
if target then
    weld(target, false)
end
end)

WeldingBinds:Bind("Weld To The Nearest Car", Enum.KeyCode.Unknown, function()
    local car = GetClosestCar()

    if car and car.PrimaryPart then
        weld(car.PrimaryPart, false)
    end
end)

WeldingBinds:Bind("Weld To The Nearest Wheel", Enum.KeyCode.Unknown, function()
    local wheel = GetClosestWheel()

    if wheel then
        weld(wheel, false)
    end
end)
